---
const services = [
{
    title: "Diseño y Desarrollo Web",
    description: "Sitios modernos, responsivos y optimizados para ofrecer una experiencia ",

  },
  {
    title: "Desarrollo de Ecommerce en Shopify",
    description: "Impulsa tu negocio online con tiendas Shopify personalizadas, seguras y fáciles de usar.",
  },

  {
    title: "Maquetación Web en Figma",
    description: "Convertimos tus ideas en diseños interactivos y visuales.",
  },
  {
    title: "Aplicaciones Web a Medida",
    description: "Soluciones personalizadas adaptadas a las necesidades de tu empresa.",
  },
  {
    title: "Chatbots con IA para Redes Sociales",
    description: "Automatiza y mejora la atención al cliente en tus redes sociales.",
  },
];

---
<section class="services-section" id="servicios">
  <div class="container">
    <!-- COLUMNA IZQUIERDA: Título, texto y botón -->
    <div class="left-content">
      <h2>Servicios</h2>
      <p>
        Descubre nuestros servicios diseñados para llevar tu negocio al siguiente nivel.
      </p>
      <button class="talk-button">Contáctanos</button>
    </div>



    <!-- COLUMNA DERECHA: Tarjetas de servicio -->
    <div class="right-content">
      <div class="services-grid">
        {services.map((service, i) => (
          <div
            class={`service-card ${i === 0 ? "accent-card" : ""} ${
              i === 1 ? "angled-card" : ""
            }`}
          >
            <h3>{service.title}</h3>
            <p>{service.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  /* Sección con fondo claro (similar a la referencia) */
  .services-section {
    background-color: #000000;
    min-height: 500px; /* según tu preferencia */
    padding: 4rem 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Contenedor principal a dos columnas (izquierda: texto, derecha: tarjetas) */
  .container {
    max-width: 1200px;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 4rem;
  }

  /* COLUMNA IZQUIERDA */
  .left-content {
    display: flex;
    flex-direction: column;
    justify-content: center; /* Centra verticalmente */
  }

  .left-content h2 {
    font-size: 3.5rem;
    font-family: var(--font-primary);
    margin-bottom: 1rem;
  }

  .left-content p {
    font-family: var(--font-primary);
    color: #999999;
    margin-bottom: 2rem;
    line-height: 1.5;
  }

  .talk-button {
    background-color: var(--color-accent, #073B3A);
    color: #000000;
    border: none;
    padding: 0.8rem 1.5rem;
    border-radius: 8px;
    cursor: pointer;
    font-family: var(--font-primary);
    font-weight: 600;
    font-size: 1rem;
    transition: background-color 0.3s ease;
  }

  .talk-button:hover {
    background-color: #05514f; /* un tono más oscuro del acento */
  }

  /* COLUMNA DERECHA: GRID DE SERVICIOS */
  .right-content {
    display: flex;
    align-items: center;
  }

  /* Grid con 3 columnas y 2 filas:
     - Fila 1: las 2 primeras tarjetas (la 2a con inclinación).
     - Fila 2: las 3 tarjetas restantes. */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: 200px; /* altura base de cada “caja” */
    gap: 1rem;
    width: 100%;
  }

  /* Posicionar las 5 tarjetas en la grid:
     1) Google Ads: top-left (col 1-2)
     2) Social media: top-right (col 2-4)
     3) Human resources: 2da fila col 1-2
     4) Project management: 2da fila col 2-3
     5) Design: 2da fila col 3-4
  */
  .service-card:nth-child(1) {
    grid-column: 1 / 2; /* solo la primera columna */
    grid-row: 1 / 2;
  }
  .service-card:nth-child(2) {
    grid-column: 2 / 4; /* abarca columna 2 y 3 */
    grid-row: 1 / 2;
  }
  .service-card:nth-child(3) {
    grid-column: 1 / 2;
    grid-row: 2 / 3;
  }
  .service-card:nth-child(4) {
    grid-column: 2 / 3;
    grid-row: 2 / 3;
  }
  .service-card:nth-child(5) {
    grid-column: 3 / 4;
    grid-row: 2 / 3;
  }

  /* Tarjeta base */
  .service-card {
    background-color: #0C0D0D;
    border-radius: 8px;
    padding: 1.5rem;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    color: #ffffff;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .service-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.15);
  }

  .service-card h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
    font-family: var(--font-primary);
  }

  .service-card p {
    font-family: var(--font-primary);
    font-size: 14px;
    line-height: 1.4;
  }

  /* Tarjeta con color de acento (la primera) */
  .accent-card {
    background-color: var(--color-accent, #073B3A);
    color: #000000;
  }

  /* Tarjeta inclinada (la segunda) */
  .angled-card {
    background-color: #0C0D0D;
    color: #ffffff;
  }

  /* Responsividad: cuando sea < 900px, las columnas se apilan */
  @media (max-width: 900px) {
    .container {
      grid-template-columns: 1fr;
    }
    .left-content {
      margin-bottom: 2rem;
      align-items: flex-start; /* o center, según tu gusto */
    }
    /* El grid de tarjetas pasa a auto-fit con 2 columnas (o 1) */
    .services-grid {
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      grid-auto-rows: auto;
    }
    /* Quitamos la posición específica para que se ordenen normal */
    .service-card:nth-child(1),
    .service-card:nth-child(2),
    .service-card:nth-child(3),
    .service-card:nth-child(4),
    .service-card:nth-child(5) {
      grid-column: auto;
      grid-row: auto;
      transform: none; /* para que la segunda no se vea rara en pantallas chicas */
    }
    .angled-card {
      transform: none; /* elimina inclinación en móviles si lo prefieres */
    }
  }
</style>
