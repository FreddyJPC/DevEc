---
const faqs = [
  {
    question: "¿Qué habilidades tienen nuestros profesionales?",
    answer:
      "Nuestro equipo está compuesto por profesionales altamente capacitados, cada uno con años de experiencia en el sector. Dominan tecnologías de vanguardia, lenguajes de programación y metodologías ágiles para ofrecer soluciones de gran calidad.",
  },
  {
    question: "¿Cuáles son los requisitos para trabajar con nosotros?",
    answer:
      "Para colaborar con nosotros, sólo necesitas contar con un proyecto claro o una idea. Nosotros te asesoramos para aterrizar tu visión y planificar la mejor estrategia de desarrollo.",
  },
  {
    question: "¿Cuál es el precio de nuestros servicios?",
    answer:
      "Nuestros precios varían según la complejidad y alcance del proyecto. Ofrecemos planes flexibles y presupuestos personalizados para cada cliente.",
  },
  {
    question: "¿Qué pasa si no me gusta el diseño que se entrega?",
    answer:
      "Trabajamos con un proceso iterativo que incluye revisiones y feedback continuo. Si algo no te convence, lo ajustamos hasta que quedes totalmente satisfecho.",
  },
];
---
<section class="faq-section" id="faqs">
  <div class="faq-container">
    <!-- COLUMNA IZQUIERDA -->
    <div class="faq-left">
      <h2>¿Tienes preguntas?</h2>
      <p>
        No te preocupes. Si tienes alguna duda o pregunta,  
        nuestras FAQs pueden ayudarte, o bien puedes ponerte  
        en contacto con nosotros directamente.
      </p>
    </div>

    <!-- COLUMNA DERECHA: LISTADO DE PREGUNTAS -->
    <div class="faq-right">
      {faqs.map((item, index) => (
        <div class="faq-item" data-index={index}>
          <div class="faq-question">
            <h3>{item.question}</h3>
            <button class="faq-toggle">
              <span class="plus-icon">+</span>
            </button>
          </div>
          <div class="faq-answer">
            <p>{item.answer}</p>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- SCRIPT para abrir/cerrar respuestas con Vanilla JS -->
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const faqItems = document.querySelectorAll(".faq-item");

      faqItems.forEach((faq) => {
        const toggleBtn = faq.querySelector(".faq-toggle");
        const answer = faq.querySelector(".faq-answer");
        const icon = faq.querySelector(".plus-icon");

        // Por defecto, ocultamos la respuesta
        answer.style.display = "none";

        // Al hacer clic en el botón (icono "+"), abrimos/cerramos la respuesta
        toggleBtn.addEventListener("click", () => {
          if (answer.style.display === "none") {
            answer.style.display = "block";
            icon.textContent = "–"; // Cambiamos el símbolo a "–"
          } else {
            answer.style.display = "none";
            icon.textContent = "+"; // Regresamos a "+"
          }
        });
      });
    });
  </script>
</section>

<style>
  /* Sección de FAQ con fondo oscuro y texto claro */
  .faq-section {
    background-color: #000; /* o #0d0d0d, según tu preferencia */
    color: #fff;
    padding: 4rem 2rem;
    min-height: 700px; /* Ajusta según tus gustos */
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Contenedor principal en 2 columnas */
  .faq-container {
    max-width: 1200px;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 3rem;
  }

  /* COLUMNA IZQUIERDA: Título y texto */
  .faq-left {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .faq-left h2 {
    font-family: var(--font-primary, sans-serif);
    font-size: 3.5rem;
    margin-bottom: 1rem;
  }

  .faq-left p {
    font-family: var(--font-primary, serif);
    line-height: 1.6;
    color: #999999;
  }

  /* COLUMNA DERECHA: Lista de preguntas */
  .faq-right {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  /* Cada pregunta y respuesta */
  .faq-item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem 0;
  }

  /* Cabecera de la pregunta con icono de +/– */
  .faq-question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: default; /* El clic se hará solo en el botón */
  }

  .faq-question h3 {
    font-family: var(--font-primary, sans-serif);
    font-size: 1.2rem;
    font-weight: 500;
    margin: 0;
  }

  /* Botón que contiene el icono +/– */
  .faq-toggle {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-white, #fff);
    font-size: 1.5rem;
    transition: color 0.3s ease;
  }
  .faq-toggle:hover {
    color: var(--color-accent, #073B3A);
  }

  .plus-icon {
    display: inline-block;
    font-weight: bold;
  }

  /* Respuesta inicialmente oculta */
  .faq-answer {
    margin-top: 0.5rem;
    line-height: 1.6;
    font-size: 0.95rem;
    color: #999999;
  }

  /* Responsividad para pantallas pequeñas */
  @media (max-width: 768px) {
    .faq-container {
      grid-template-columns: 1fr; /* Una sola columna */
    }

    .faq-left {
      margin-bottom: 2rem;
      text-align: center;
    }

    .faq-left h2 {
      font-size: 2rem;
    }
  }
</style>
