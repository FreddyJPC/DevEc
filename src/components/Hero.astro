---
// Si usas Frontmatter, puedes incluir variables u otros elementos aquí.
---
<section class="hero-section">
  <div class="hero-content">
    <h1>
      Convertimos tus ideas en <span class="font-secondary-italic">realidad</span>
    </h1>
    <p>
      Diseñamos y desarrollamos soluciones digitales que impulsan tu negocio.
    </p>
    <button>Agenda una llamada</button>
  </div>
  <div class="hero-images">
    <!-- Collage de imágenes -->
    <div class="collage">
      <img src="/img/Img2.png" alt="Imagen 2" class="img-2" />
      <!-- <img src="/img/Img5.png" alt="Imagen 5" class="img-5" /> -->
      <img src="/img/Img6.png" alt="Imagen 6" class="img-6" />
      <img src="/img/Img2.png" alt="Imagen 2" class="img-2" />
      <img src="/img/Img4.png" alt="Imagen 4" class="img-4" />
    </div>
  </div>
</section>

<style>
  :root {
    --font-primary: 'Satoshi', sans-serif;
    --font-extended: 'Chaney Extended', sans-serif;
    --font-secondary: 'Instrument Serif', sans-serif;
    --color-accent: #00E599;
    --color-white: #fff;
  }

  /* Fuente secundaria */
  .font-secondary {
    font-family: var(--font-secondary);
  }

  .hero-section {  
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 100vh;
    padding: 4rem 8rem;
    animation: fadeInUp 0.8s ease-in-out;
  }

  .hero-content {
    max-width: 50%;
    text-align: left;
    padding: 0 2rem;
  }

  .hero-content h1 {
    font-weight: 500;
    font-family: var(--font-primary);
    font-size: 80px;
    margin-bottom: 1rem;
    line-height: 1.1;
  }

  .hero-content p {
    color: #999999;
    font-family: var(--font-primary);
    font-size: 1.25rem;
    margin-bottom: 2rem;
  }

  button {
    background-color: var(--color-accent);
    color: #000;
    border: none;
    padding: 1rem 2rem;
    font-family: var(--font-primary);
    font-weight: 600;
    cursor: pointer;
    border-radius: 24px;
    transition: background-color 0.3s ease;
  }

  button:hover {
    background-color: #00b584;
  }

  .hero-images {
    max-width: 55%;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .collage {
    position: relative;
    width: 500px;
    height: 500px;
  }

  .collage img {
    position: absolute;
    object-fit: cover;
    border-radius: 20px;
    transition: transform 0.3s ease;
  }

  /* Posiciones y rotaciones para el collage */
  .img-2 {
    width: 500px;
    height: 400px;
    top: 0;
    left: 20px;
    transform: rotate(8deg);
    box-shadow: 0 4px 15px rgba(255, 255, 255, 0.5);
  }

  .img-4 {
    width: 300px;
    height: 600px;
    bottom: 0;
    right: 200px;
    transform: rotate(-8deg);
    filter: drop-shadow(0 4px 15px rgba(255, 255, 255, 0.5));
  }

  .img-6 {
    width: 500px;
    height: 400px;
    bottom: 10%;
    transform: translateX(-50%) rotate(-3deg);
    box-shadow: 0 4px 15px rgba(255, 255, 255, 0.5);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Estilos responsivos para pantallas pequeñas */
  @media (max-width: 768px) {
    .hero-section {
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem 1rem;
      min-height: auto;
    }

    .hero-content {
      max-width: 100%;
      text-align: center;
      padding: 0 1rem;
      margin-bottom: 2rem;
    }

    .hero-images {
      max-width: 100%;
      margin-bottom: 2rem;
    }

    .collage {
      width: 100%;
      height: auto;
      position: relative;
    }

    .collage img {
      max-width: 70%;
      height: auto;
    }

    /* Reducir transformaciones para evitar sobresalir */
    .img-2 {
      transform: rotate(4deg);
      top: 0;
      left: 0;
    }
    .img-4 {
      transform: rotate(-4deg);
      bottom: 0;
      right: 0;
    }
    .img-6 {
      transform: rotate(-2deg);
      bottom: 20%;
      left: 15%;
    }
  }
</style>
