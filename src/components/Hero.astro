<section class="hero-section">
  <div class="hero-content">
    <h1>
      Convertimos tus ideas en <span class="font-secondary-italic">realidad</span>
    </h1>
    <p>
      Diseñamos y desarrollamos soluciones digitales que impulsan tu negocio.
    </p>
    <button>Agenda una llamada</button>
  </div>

  <div class="hero-images">
    <img
      src="/img/hero-collage-combined.png"
      alt="Devec showcase de proyectos y servicios"
      class="hero-single-image"
    />
  </div>
</section>

<style>
  :root {
    --font-primary: 'Satoshi', sans-serif;
    --font-secondary: 'Instrument Serif', serif;
    --color-accent: #00E599;
  }

  body {
    margin: 0;
    overflow-x: hidden; /* Previene scroll horizontal no deseado */
  }

  .hero-section {
    display: flex;
    align-items: center;
    justify-content: space-between;
    /* Usamos gap para separar el texto de la imagen */
    gap: 2rem;
    padding: clamp(2rem, 8vw, 8rem);
    min-height: 90vh; /* Un poco menos de 100vh a veces se ve mejor */
    box-sizing: border-box;
    z-index: 999;
    position: relative

  }

  /* --- CONTENIDO DE TEXTO (IZQUIERDA) --- */
  .hero-content {
    flex: 1 1 400px; /* Crece y se encoge, base de 400px */
    max-width: 800px;
    animation: fadeInUp 0.8s ease-in-out;
  }

  .hero-content h1 {
    font-family: var(--font-primary);
    /* Tipografía fluida: se adapta suavemente entre 3rem y 4.7rem */
    font-size: clamp(3rem, 6vw, 4.7rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: white;
  }

  .font-secondary-italic {
    font-family: var(--font-secondary);
    font-style: italic;
    font-weight: 400;
    color: var(--color-accent); /* Un toque de color al "realidad" */
  }

  .hero-content p {
    font-family: var(--font-primary);
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    color: #a0a0a0; /* Un gris más suave para fondo oscuro */
    margin-bottom: 2.5rem;
    line-height: 1.6;
    max-width: 90%;
  }

  .hero-content button {
    background-color: var(--color-accent);
    color: #000;
    border: none;
    padding: 1rem 2.5rem;
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: 1rem;
    border-radius: 100px; /* Botón completamente redondeado */
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .hero-content button:hover {
    background-color: #00b584;
    transform: translateY(-3px); /* Pequeño efecto de elevación */
    box-shadow: 0 10px 20px -10px rgba(0, 229, 153, 0.5);
  }

  /* --- SECCIÓN DE LA IMAGEN ÚNICA (DERECHA) --- */
  .hero-images {
    flex: 1 400px;
    display: flex;
    justify-content: flex-end; /* Alinea la imagen a la derecha en desktop */
    align-items: center;
    animation: fadeInRight 1s ease-out;
  }

  /* Estilos para la nueva imagen única */
  .hero-single-image {
    width: 100%; /* Ocupa el ancho disponible de su contenedor */
    height: auto; /* Mantiene la proporción siempre */
    max-width: 650px; /* Evita que se haga gigantesca en pantallas muy anchas */
    object-fit: contain;
    /* Opcional: un sutil efecto hover */
    transition: transform 0.5s ease;
  }

  .hero-single-image:hover {
      transform: scale(1.02); /* Un "respiro" muy sutil al pasar el mouse */
  }


  /* --- ANIMACIONES --- */
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeInRight {
      from { opacity: 0; transform: translateX(30px); }
      to { opacity: 1; transform: translateX(0); }
  }


  /* --- RESPONSIVE: TABLET (Hasta 1024px) --- */
  @media (max-width: 1024px) {
    .hero-section {
      padding: 4rem 2rem;
      min-height: auto; /* Ya no forzamos la altura completa */
    }
    .hero-content h1 {
      font-size: 3.5rem; /* Ajuste manual si clamp no es suficiente */
    }
  }


  /* --- RESPONSIVE: MÓVIL (Hasta 768px) --- */
  @media (max-width: 768px) {
    .hero-section {
      flex-direction: column; /* IMPORTANTE: Ponemos la imagen arriba del texto en móvil */
      padding: 6rem 1.5rem 3rem 1.5rem; /* Padding superior para tu navbar */
      text-align: center;
      gap: 0rem;
    }

    .hero-content {
      padding: 0;
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center; /* Centramos textos y botón */
    }

    .hero-content p {
        max-width: 100%;
    }

    .hero-images {
      justify-content: center; /* Centramos la imagen horizontalmente */
      width: 100%;
    }

    .hero-single-image {
      max-width: 100%; /* En móvil, que no toque los bordes exactos */
    }
  }
</style>